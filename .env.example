# safespeak-admin is local-first and does not connect to any backend or remote
# database in this phase. All content lives in the browser's IndexedDB store.
#
# This file is a placeholder for future, explicitly-approved configuration
# (for example, a future API base URL once a real backend repository
# implementation is introduced). No variables are required to run the app today.
